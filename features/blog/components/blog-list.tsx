import React from 'react';
import
type BlogListProps = {
    blogs:Blog[];
    uvMap?: Record<string,number>;
}
export default function BlogList({blogs,uvMap}) {
    return (
        <div></div>
    );
}

